//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = Color.fromARGB(255, 30, 38, 44);
  static Color secondaryColor = Color.fromARGB(255, 78, 100, 114);
  static Color accentColor = Color.fromARGB(255, 194, 206, 213);
}
